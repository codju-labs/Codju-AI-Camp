---
title: "Music & Showcase"
sectionCount: 4
---

import { ProjectDeck } from "../../components/ui/ProjectDeck";
import { ArtistProject } from "../../components/ui/ArtistProject";
import { SectionWrapper } from "../../components/learning/SectionWrapper";
import { RevealButton } from "../../components/ui/RevealButton";
import { ConceptCard } from "../../components/ui/ConceptCard";

{/* -- Section 0: Hero -- */}
<SectionWrapper index={0} client:load>
  <style>{`
    @keyframes heroSlideUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes bob { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-8px); } }
    .hero-e1 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) both; }
    .hero-e2 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.12s both; }
    .hero-e3 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.24s both; }
    .hero-e4 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.36s both; }
    .b1 { animation: bob 2.2s ease-in-out infinite; }
    @media (prefers-reduced-motion: reduce) { .hero-e1,.hero-e2,.hero-e3,.hero-e4 { animation-duration: 0.01s; } .b1 { animation: none; } }
  `}</style>
  <div className="text-center py-14 min-h-[60vh] flex flex-col items-center justify-center">
    <div className="hero-e1 mb-8 flex items-center justify-center">
      <span className="b1 flex h-20 w-20 items-center justify-center rounded-3xl bg-amber-500 text-2xl font-black text-white shadow-md">Su</span>
    </div>
    <h1 className="hero-e2 text-5xl font-black text-slate-900 mb-4">Music &amp; Showcase</h1>
    <p className="hero-e3 text-xl md:text-2xl text-slate-500 font-bold mb-9 max-w-xl mx-auto leading-relaxed">
      Give your hero a soundtrack on Suno, then put it all together.
    </p>
    <div className="hero-e4">
      <RevealButton sectionIndex={0} text="Make some music" client:load />
    </div>
  </div>
</SectionWrapper>

{/* -- Section 1: The music project deck -- */}
<SectionWrapper index={1} client:load>
  <style>{`
    @keyframes fadeUp { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }
    .fade-up { animation: fadeUp 0.6s cubic-bezier(0.16,1,0.3,1) both; }
    @media (prefers-reduced-motion: reduce) { .fade-up { animation: none; } }
  `}</style>
  <div className="text-center mb-7 fade-up">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">Make these on Suno</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">Score Your Hero</h2>
    <p className="text-xl md:text-2xl text-slate-500 font-bold max-w-xl mx-auto">
      Open Suno, describe a song, and let it sing.
    </p>
  </div>
  <div className="fade-up">
    <ProjectDeck
      client:only="react"
      sectionIndex={1}
      deck="music"
      explanation="For music, describe the feeling: the style, the mood, and what the song is about. Suno writes the music, the words, and the vocals."
    />
  </div>
</SectionWrapper>

{/* -- Section 2: Capstone — put it all together -- */}
<SectionWrapper index={2} client:load>
  <style>{`
    @keyframes scaleIn { from { opacity: 0; transform: scale(0.92) translateY(12px); } to { opacity: 1; transform: scale(1) translateY(0); } }
    .scale-in { animation: scaleIn 0.5s cubic-bezier(0.175,0.885,0.32,1.275) both; }
    @media (prefers-reduced-motion: reduce) { .scale-in { animation: none; } }
  `}</style>
  <div className="text-center mb-7 scale-in">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">The big one</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">Bring It All Together</h2>
    <p className="text-xl md:text-2xl text-slate-500 font-bold max-w-xl mx-auto">
      Fill your hero's card and earn the AI Artist badge.
    </p>
  </div>
  <div className="scale-in">
    <ArtistProject
      client:only="react"
      sectionIndex={2}
      explanation="Your hero now has art, designs, and a song. This card pulls it together into one shareable creation."
    />
  </div>
</SectionWrapper>

{/* -- Section 3: Reflection + outro -- */}
<SectionWrapper index={3} client:load>
  <style>{`
    @keyframes fadeUp { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }
    .fade-up { animation: fadeUp 0.6s cubic-bezier(0.16,1,0.3,1) both; }
    @media (prefers-reduced-motion: reduce) { .fade-up { animation: none; } }
  `}</style>
  <div className="text-center mb-7 fade-up">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">Day 2 complete</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">You Are an AI Artist</h2>
  </div>
  <div className="fade-up">
    <ConceptCard icon="GO" title="One creator's rule before you go">
      <p className="text-lg mb-3">
        Make as much as you want — but always be honest. When you share AI art or music, <strong>say it was made with AI</strong>, and never make fake images of real people.
      </p>
      <div className="rounded-2xl bg-[#faf5ff] border-2 border-[#e9d5ff] px-4 py-3 text-center">
        <p className="font-black text-[#6b21a8] text-lg">Homework: finish your comic and theme song at home, with a parent.</p>
      </div>
    </ConceptCard>
    <p className="text-center text-xl md:text-2xl text-slate-500 font-bold mt-6">
      Tomorrow: become a research detective who finds true facts and checks what is real.
    </p>
  </div>
  <RevealButton sectionIndex={3} text="Claim Day 2" client:load />
</SectionWrapper>
