---
title: "Creating with AI"
sectionCount: 5
---

import { SayItSeeIt } from "../../components/ui/SayItSeeIt";
import { PainterStory } from "../../components/ui/PainterStory";
import { SectionWrapper } from "../../components/learning/SectionWrapper";
import { RevealButton } from "../../components/ui/RevealButton";
import { ConceptCard } from "../../components/ui/ConceptCard";

{/* -- Section 0: Hero -- */}
<SectionWrapper index={0} client:load>
  <style>{`
    @keyframes heroSlideUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes bob { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-8px); } }
    .hero-e1 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) both; }
    .hero-e2 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.12s both; }
    .hero-e3 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.24s both; }
    .hero-e4 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.36s both; }
    .b1 { animation: bob 2.2s ease-in-out infinite; }
    .b2 { animation: bob 2.2s ease-in-out 0.3s infinite; }
    .b3 { animation: bob 2.2s ease-in-out 0.6s infinite; }
    @media (prefers-reduced-motion: reduce) { .hero-e1,.hero-e2,.hero-e3,.hero-e4 { animation-duration: 0.01s; } .b1,.b2,.b3 { animation: none; } }
  `}</style>
  <div className="text-center py-14 min-h-[60vh] flex flex-col items-center justify-center">
    <div className="hero-e1 mb-8 flex items-center gap-4">
      <span className="b1 rounded-2xl bg-emerald-100 px-6 py-5 text-xl font-black text-emerald-700">DRAW</span>
      <span className="b2 rounded-2xl bg-blue-100 px-6 py-5 text-xl font-black text-blue-700">DESIGN</span>
      <span className="b3 rounded-2xl bg-violet-100 px-6 py-5 text-xl font-black text-violet-700">SING</span>
    </div>
    <h1 className="hero-e2 text-5xl font-black text-slate-900 mb-4">Creating with AI</h1>
    <p className="hero-e3 text-xl md:text-2xl text-slate-500 font-bold mb-9 max-w-md mx-auto leading-relaxed">
      Yesterday you talked to AI. Today you make it create.
    </p>
    <div className="hero-e4">
      <RevealButton sectionIndex={0} text="Let's create" client:load />
    </div>
  </div>
</SectionWrapper>

{/* -- Section 1: Hook — Say It, See It -- */}
<SectionWrapper index={1} client:load>
  <style>{`
    @keyframes scaleIn { from { opacity: 0; transform: scale(0.92) translateY(12px); } to { opacity: 1; transform: scale(1) translateY(0); } }
    .scale-in { animation: scaleIn 0.5s cubic-bezier(0.175,0.885,0.32,1.275) both; }
    @media (prefers-reduced-motion: reduce) { .scale-in { animation: none; } }
  `}</style>
  <div className="text-center mb-7 scale-in">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">Try it</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">Say It, See It</h2>
    <p className="text-xl md:text-2xl text-slate-500 font-bold max-w-xl mx-auto">
      Describe a picture and watch AI build it.
    </p>
  </div>
  <div className="scale-in">
    <SayItSeeIt
      client:only="react"
      sectionIndex={1}
      explanation="The AI never changed. Only your words did. A vague prompt has nothing to grab onto, so it gives you something generic. A specific prompt — subject, style, scene, details — tells it exactly what to build. Describe more, get more."
    />
  </div>
</SectionWrapper>

{/* -- Section 2: Concept — Generative media -- */}
<SectionWrapper index={2} client:load>
  <style>{`
    @keyframes fadeUp { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }
    .fade-up { animation: fadeUp 0.6s cubic-bezier(0.16,1,0.3,1) both; }
    @media (prefers-reduced-motion: reduce) { .fade-up { animation: none; } }
  `}</style>
  <div className="text-center mb-7 fade-up">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">The big idea</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">Generative Media</h2>
    <p className="text-xl md:text-2xl text-slate-500 font-bold max-w-xl mx-auto">
      Same trick as yesterday — new materials.
    </p>
  </div>
  <div className="fade-up">
    <ConceptCard icon="G" title="Generative media">
      <p className="text-lg mb-2">Yesterday you learned: text AI guesses the next <strong>word</strong>.</p>
      <p className="text-lg mb-4">Image, music and video AI work the same way — only with pixels and sounds.</p>
      <div className="rounded-2xl bg-[#faf5ff] border-2 border-[#e9d5ff] px-4 py-4 text-center">
        <p className="font-black text-[#6b21a8] text-xl">It does NOT search for an existing picture.</p>
      </div>
      <p className="text-lg mt-4">It builds a brand-new one, just for you, from the millions of examples it learned from.</p>
    </ConceptCard>
  </div>
  <RevealButton sectionIndex={2} text="Meet a painter who can't draw" client:load />
</SectionWrapper>

{/* -- Section 3: Story — The Artist Who Could Only Describe -- */}
<SectionWrapper index={3} client:load>
  <style>{`
    @keyframes scaleIn { from { opacity: 0; transform: scale(0.92) translateY(12px); } to { opacity: 1; transform: scale(1) translateY(0); } }
    .scale-in { animation: scaleIn 0.5s cubic-bezier(0.175,0.885,0.32,1.275) both; }
    @media (prefers-reduced-motion: reduce) { .scale-in { animation: none; } }
  `}</style>
  <div className="text-center mb-7 scale-in">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">Story</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">The Artist Who Could Only Describe</h2>
    <p className="text-xl md:text-2xl text-slate-500 font-bold max-w-xl mx-auto">
      Maya can't draw a line. Watch her make art anyway.
    </p>
  </div>
  <div className="scale-in">
    <PainterStory
      client:only="react"
      sectionIndex={3}
      explanation="Maya never picked up a brush. Her whole skill was describing what she imagined, clearly and fully. That is exactly your job with AI: you are the describer, and your description is the art."
    />
  </div>
</SectionWrapper>

{/* -- Section 4: Wrap -- */}
<SectionWrapper index={4} client:load>
  <style>{`
    @keyframes fadeUp { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }
    .fade-up { animation: fadeUp 0.6s cubic-bezier(0.16,1,0.3,1) both; }
    @media (prefers-reduced-motion: reduce) { .fade-up { animation: none; } }
  `}</style>
  <div className="text-center mb-7 fade-up">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">Lock it in</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">You Are the Describer Now</h2>
  </div>
  <div className="fade-up">
    <ConceptCard icon="GO" title="You are the describer now">
      <p className="text-lg mb-2">Better description, better creation.</p>
      <p className="text-lg mb-2"><strong>Vague in,</strong> generic out.</p>
      <p className="text-lg mb-4"><strong>Specific in,</strong> stunning out.</p>
      <div className="rounded-2xl bg-[#fff7ed] border-2 border-[#fed7aa] px-4 py-4 text-center">
        <p className="font-black text-[#b45309] text-xl">Next: meet your creative toolkit — the studios that draw, design, and sing.</p>
      </div>
    </ConceptCard>
  </div>
  <RevealButton sectionIndex={4} text="Finish Lesson 1" client:load />
</SectionWrapper>
