---
title: "Meet DPAA"
sectionCount: 3
---

import { DpaaWalk } from "../../components/ui/DpaaWalk";
import { SectionWrapper } from "../../components/learning/SectionWrapper";
import { RevealButton } from "../../components/ui/RevealButton";
import { ConceptCard } from "../../components/ui/ConceptCard";

{/* -- Section 0: Hero -- */}
<SectionWrapper index={0} client:load>
  <style>{`
    @keyframes heroSlideUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes bob { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-8px); } }
    .hero-e1 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) both; }
    .hero-e2 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.12s both; }
    .hero-e3 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.24s both; }
    .hero-e4 { animation: heroSlideUp 0.7s cubic-bezier(0.16,1,0.3,1) 0.36s both; }
    .b1 { animation: bob 2.2s ease-in-out infinite; }
    .b2 { animation: bob 2.2s ease-in-out 0.3s infinite; }
    .b3 { animation: bob 2.2s ease-in-out 0.6s infinite; }
    .b4 { animation: bob 2.2s ease-in-out 0.9s infinite; }
    @media (prefers-reduced-motion: reduce) { .hero-e1,.hero-e2,.hero-e3,.hero-e4 { animation-duration: 0.01s; } .b1,.b2,.b3,.b4 { animation: none; } }
  `}</style>
  <div className="text-center py-14 min-h-[60vh] flex flex-col items-center justify-center">
    <div className="hero-e1 mb-8 flex items-center gap-3">
      <span className="b1 flex h-16 w-16 items-center justify-center rounded-2xl bg-[#8B4EC4] text-3xl font-black text-white shadow-md">D</span>
      <span className="b2 flex h-16 w-16 items-center justify-center rounded-2xl bg-blue-500 text-3xl font-black text-white shadow-md">P</span>
      <span className="b3 flex h-16 w-16 items-center justify-center rounded-2xl bg-emerald-500 text-3xl font-black text-white shadow-md">A</span>
      <span className="b4 flex h-16 w-16 items-center justify-center rounded-2xl bg-amber-500 text-3xl font-black text-white shadow-md">A</span>
    </div>
    <h1 className="hero-e2 text-5xl font-black text-slate-900 mb-4">Meet DPAA</h1>
    <p className="hero-e3 text-xl md:text-2xl text-slate-500 font-bold mb-9 max-w-xl mx-auto leading-relaxed">
      Four thinking moves that crack any problem. Let's learn them with a pizza party.
    </p>
    <div className="hero-e4">
      <RevealButton sectionIndex={0} text="Start thinking" client:load />
    </div>
  </div>
</SectionWrapper>

{/* -- Section 1: The DPAA walkthrough -- */}
<SectionWrapper index={1} client:load>
  <style>{`
    @keyframes fadeUp { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }
    .fade-up { animation: fadeUp 0.6s cubic-bezier(0.16,1,0.3,1) both; }
    @media (prefers-reduced-motion: reduce) { .fade-up { animation: none; } }
  `}</style>
  <div className="text-center mb-7 fade-up">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">The four moves</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">Plan the Perfect Pizza Party</h2>
    <p className="text-xl md:text-2xl text-slate-500 font-bold max-w-xl mx-auto">
      Walk through D, P, A, A — one move at a time.
    </p>
  </div>
  <div className="fade-up">
    <DpaaWalk
      client:only="react"
      sectionIndex={1}
      explanation="DPAA = Decomposition (break it up), Pattern recognition (spot repeats), Abstraction (keep what matters), Algorithm (put it in order). The same four moves work on any problem."
    />
  </div>
</SectionWrapper>

{/* -- Section 2: Wrap -- */}
<SectionWrapper index={2} client:load>
  <style>{`
    @keyframes fadeUp { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }
    .fade-up { animation: fadeUp 0.6s cubic-bezier(0.16,1,0.3,1) both; }
    @media (prefers-reduced-motion: reduce) { .fade-up { animation: none; } }
  `}</style>
  <div className="text-center mb-7 fade-up">
    <p className="text-sm font-black text-slate-400 uppercase tracking-widest mb-3">You've got it</p>
    <h2 className="text-4xl font-black text-slate-800 mb-2 tracking-tight">The DPAA Toolkit</h2>
  </div>
  <div className="fade-up">
    <ConceptCard icon="D" title="Four moves, any problem">
      <p className="text-lg mb-2"><strong>D</strong> — break it into parts.</p>
      <p className="text-lg mb-2"><strong>P</strong> — spot what repeats.</p>
      <p className="text-lg mb-2"><strong>A</strong> — keep what matters.</p>
      <p className="text-lg mb-4"><strong>A</strong> — put the steps in order.</p>
      <div className="rounded-2xl bg-[#fff7ed] border-2 border-[#fed7aa] px-4 py-3 text-center">
        <p className="font-black text-[#b45309] text-lg">This is how computer scientists think — and how you'll plan your website.</p>
      </div>
    </ConceptCard>
    <p className="text-center text-xl md:text-2xl text-slate-500 font-bold mt-6">
      Next: point DPAA at YOUR own idea.
    </p>
  </div>
  <RevealButton sectionIndex={2} text="Finish Lesson 1" client:load />
</SectionWrapper>
